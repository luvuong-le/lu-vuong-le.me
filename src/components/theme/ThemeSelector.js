import React, { useState, useEffect } from "react"

/**
 * Theme Selector: UI Component for selecting theme
 */
export default function ThemeSelector() {
    const defaultTheme = "light"

    const toggleTheme = () => {}

    return (
        <>
            <h1>Theme Selector</h1>
        </>
    )
}
